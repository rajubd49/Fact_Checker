# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Fact Checker' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Fact Checker
  pod 'Firebase'
  pod 'Firebase/Auth'
  pod 'Firebase/Database'
  pod 'Firebase/Firestore'

  target 'Fact CheckerTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'Fact CheckerUITests' do
    # Pods for testing
  end

end
